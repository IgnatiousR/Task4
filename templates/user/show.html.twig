{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
<div class="mt-10 px-6">
    <h1 class="text-3xl font-semibold text-gray-600">Your Info</h1>
</div>
<div class="mt-5">
    <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
        <div class="bg-white overflow-hidden shadow-sm rounded-lg pb-6">
            <div class="p-6">
                <div class="flex justify-between">
                    <img class="h-[128px] rounded-full border" 
                    src="{{asset('images/default-profile-pic.jpg')}}" alt="Default pic">
                    <div>
                        {% if user.id == app.user.id %}
                            <a href="{{path('app_user_edit', {id:user.id})}}" type="button" class="inline-flex items-center px-4 py-2 bg-white border border-gray-300 rounded-md font-semibold text-xs text-gray-700 uppercase tracking-widest shadow-sm hover:bg-gray-50 focus:outline-none disabled:opacity-25 transition ease-in-out duration-150"
                            >Edit Profile</a>
                        {% endif %}
                    </div>
                </div>
                <div class="mt-6 flex justify-start border-b pb-4 border-gray-300">
                    <button type="button" class="hidden inline-flex items-center px-4 py-2 bg-white border border-gray-300 rounded-md font-semibold text-xs text-gray-700 uppercase tracking-widest shadow-sm hover:bg-gray-50 disabled:opacity-25 transition ease-in-out duration-150"
                    >Change Photo</button>
                </div>
            </div>
            <div class="px-6 flex space-x-20 text-sm">
                <div class=" text-gray-900">Full name:</div>
                <div class=" text-gray-600 font-semibold">{{user.firstName}} {{user.lastName}}</div>
            </div>
            <div class="px-6 flex space-x-20 text-sm">
                <div class=" text-gray-900">Email:</div>
                <div class=" text-gray-600 font-semibold">{{user.email}}</div>
            </div>
            <div class="px-6 flex space-x-20 text-sm">
                <div class=" text-gray-900">Status:</div>
                <div class=" text-gray-600 font-semibold">{{user.status}}</div>
            </div>
            <div class="px-6 flex space-x-20 text-sm">
                <div class=" text-gray-900">UUID:</div>
                <div class=" text-gray-600 font-semibold">{{user.uuid}}</div>
            </div>
            {# <div class="p-6 text-gray-900">
                You're logged in!
                <a class="hover:text-stone-500" href="{{path('app_user_edit', {id:user.id})}}">Profile Edit</a>
                <div class="mt-6" x-data="{tab:1}">
                    <div class="flex mx-2 mb-4 space-x-4 text-xl border-b border-gray-300">
                        <div class="hover:text-emerald-500 py-2 cursor-pointer"
                        :class="{ 'text-emerald-500 border-b border-emerald-500': tab == 1 }" @click="tab = 1">Followers</div>

                        <div class="hover:text-emerald-500 py-2 cursor-pointer"
                        :class="{ 'text-emerald-500 border-b border-emerald-500': tab == 2 }" @click="tab = 2">Following</div>

                        <div class="hover:text-emerald-500 py-2 cursor-pointer"
                        :class="{ 'text-emerald-500 border-b border-emerald-500': tab == 3 }" @click="tab = 3">Liked Posts</div>
                    </div>
                    <div x-show="tab === 1">
                        <b>People that follow you:</b>
                        <ul>
                            <li>
                                <a class="hover:text-stone-500" href="">John Smith</a>
                            </li>
                        </ul>
                    </div>
                    <div x-show="tab === 2">
                        <b>People that you follow:</b>
                        <ul class="space-y-4">
                            <li>
                                <a class="hover:text-stone-500" href="">Jane Doe</a>
                                <a href="" class="ml-3 inline font-bold text-sm px-4 py-1 text-white rounded bg-blue-500 hover:bg-blue-600">Unfollow</a>
                            </li>
                        </ul>
                    </div>
                    <div x-show="tab === 3">
                        <b>Posts you liked:</b>
                        <ul>
                            <li>
                                <a class="hover:text-stone-500" href="">Liked post title</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div> #}
        </div>
    </div>
</div>
{% endblock %}